CREATE TABLE championship (
                              id SERIAL PRIMARY KEY,
                              name VARCHAR(100) NOT NULL,
                              country VARCHAR(100) NOT NULL
);